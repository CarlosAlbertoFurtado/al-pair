[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && npx prisma generate && npx prisma db push && npx tsc"

[deploy]
startCommand = "cd server && node dist/server.js"
restartPolicyType = "on_failure"
